{
  "name": "9router-api",
  "version": "0.5.50",
  "description": "Standalone 9Router API server - lightweight LLM proxy without dashboard UI",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "tsx server.ts",
    "build": "esbuild server.ts --bundle --platform=node --outfile=dist/server.js --format=esm --external:9router --external:bun:sqlite --external:better-sqlite3",
    "dev": "tsx watch server.ts"
  },
  "dependencies": {
    "express": "^5.2.1",
    "jose": "^6.1.3",
    "sql.js": "^1.14.1",
    "undici": "^7.19.2",
    "uuid": "^13.0.0"
  },
  "devDependencies": {
    "esbuild": "^0.28.2",
    "tsx": "^4.19.0"
  }
}
